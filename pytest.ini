[pytest]
testpaths = tests
markers =
    integration: tests that exercise the application pipeline with real infrastructure
    e2e: end-to-end tests that require Telegram bot wiring
addopts =
    --cov=mergemate
    --cov-report=term-missing
    --cov-report=xml